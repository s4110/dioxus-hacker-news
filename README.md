# Hacker News clone using Dioxus

This repository is for self-learning of the framework.

Please install `dioxus-cli` before proceeding with the next steps.

```shell
$ cargo install dioxus-cli
```

Start develop server

```shell
$ pnpm install

$ pnpm dev
```